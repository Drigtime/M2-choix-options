{% extends 'base.html.twig' %}

{% block title %}CampagneChoix index{% endblock %}

{% block body %}
    <h1>CampagneChoix index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateDebut</th>
                <th>DateFin</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for campagne_choix in campagne_choixes %}
            <tr>
                <td>{{ campagne_choix.id }}</td>
                <td>{{ campagne_choix.dateDebut ? campagne_choix.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ campagne_choix.dateFin ? campagne_choix.dateFin|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_campagne_choix_show', {'id': campagne_choix.id}) }}">show</a>
                    <a href="{{ path('app_campagne_choix_edit', {'id': campagne_choix.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_campagne_choix_new') }}">Create new</a>
{% endblock %}
