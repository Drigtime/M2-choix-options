<form method="post" action="{{ path('app_bloc_ue_delete', {'id': bloc_ue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bloc_ue.id) }}">
    <button class="btn">Delete</button>
</form>
