<form method="post" action="{{ path('app_bloc_ue_category_delete', {'id': bloc_u_e_category.id}) }}"
      onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bloc_u_e_category.id) }}">
    <button class="btn btn-sm btn-danger">
        <i class="fa fa-trash"></i>
        Supprimer
    </button>
</form>
