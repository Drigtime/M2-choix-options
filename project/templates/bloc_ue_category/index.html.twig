{% extends 'base.html.twig' %}

{% block title %}BlocUECategory index{% endblock %}

{% block body %}
    <h1>BlocUECategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bloc_u_e_category in bloc_u_e_categories %}
            <tr>
                <td>{{ bloc_u_e_category.id }}</td>
                <td>{{ bloc_u_e_category.label }}</td>
                <td>
                    <a href="{{ path('app_bloc_ue_category_show', {'id': bloc_u_e_category.id}) }}">show</a>
                    <a href="{{ path('app_bloc_ue_category_edit', {'id': bloc_u_e_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_bloc_ue_category_new') }}">Create new</a>
{% endblock %}
