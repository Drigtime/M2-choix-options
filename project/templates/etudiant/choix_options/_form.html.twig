{% use 'project\src\Form\ChoixType.php' %}
{% set form = formFactory.create(ChoixType::class) %}
{{ form_start(form) }}
{{ form_widget(form) }}

<div class="text-end">
	<button class="btn btn-sm btn-primary">
		<i class="fas fa-save"></i>
		{{ button_label|default('Enregistrer') }}
	</button>
</div>
{{ form_end(form) }}
