{% extends 'base.html.twig' %}

{% block title %}Parcours{% endblock %}

{% block body %}
    <h1>Parcours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parcour.id }}</td>
            </tr>
            <tr>
                <th>Année de formation</th>
                <td>{{ parcour.anneeFormation }}</td>
            </tr>
            <tr>
                <th>Rythme</th>
                <td>{{ parcour.rythme }}</td>
            </tr>
            <tr>
                <th>Specialisation</th>
                <td>{{ parcour.specialisation }}</td>
            </tr>
            <tr>
                <th>Bloc UEs</th>
                <td>
                    {% for blocUE in parcour.blocUEs %}
                        <a href="{{ path('app_bloc_ue_show', {'id': blocUE.id}) }}">{{ blocUE.label }}</a>
                    {% endfor %}
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parcours_index') }}">back to list</a>

    <a href="{{ path('app_parcours_edit', {'id': parcour.id}) }}">edit</a>

    {{ include('parcours/_delete_form.html.twig') }}
{% endblock %}
