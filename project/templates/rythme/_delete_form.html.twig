<form method="post" action="{{ path('app_rythme_delete', {'id': rythme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rythme.id) }}">
    <button class="btn">Delete</button>
</form>
