<form method="post" action="{{ path('app_annee_formation_delete', {'id': annee_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annee_formation.id) }}">
    <button class="btn">Delete</button>
</form>
